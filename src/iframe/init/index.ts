// in-iframe initialisation entry point (IIFE build via vite.iframe-init.config.ts).
// Tasks 2.3 / 2.4 / 2.5 / 3.1 will fill in RequestManager, FrameGlobals, and FrameMessenger.
// This placeholder keeps the build pipeline wired without any observable side-effects.
(function iframeInitPlaceholder() {
  // intentionally empty — replaced in later tasks
  console.debug("[drawio-frame] iframe-init placeholder loaded");
})();
